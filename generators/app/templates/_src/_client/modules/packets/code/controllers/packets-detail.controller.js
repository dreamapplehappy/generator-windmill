'use strict';

import '../styles/packets-detail.scss';

class PacketsDetailController {
    constructor() {

    }
}

// 在这里,我们将控制器作为一个模块导出
export default angular
    .module('packets-detail.controller', [])
    .controller('PacketsDetailController', PacketsDetailController);